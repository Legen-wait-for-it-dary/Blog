@model IEnumerable<Blog.WEB.UI.Models.Category>
@*<div class="tags">
    <ul>
        @foreach (var item in Model)
        {
            <li>@Html.ActionLink(@item.Name, "Index", new { @categoryName = @item.Name }, null) </li>
        }
    </ul>
</div>*@
